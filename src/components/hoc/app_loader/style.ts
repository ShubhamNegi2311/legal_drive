/* REACT */
import {StyleSheet} from 'react-native';

/** CUSTOM MODULES */
import {LOADER_BACKGROUND} from 'styles/colors';

export const style = StyleSheet.create({
  mainContainer: {
    flex: 1,
    backgroundColor: LOADER_BACKGROUND,
  },
});
